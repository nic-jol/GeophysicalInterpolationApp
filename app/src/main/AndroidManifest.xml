<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.violetfire.geophysicalinterpolationapp">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>  // Redundant

    <application
        android:name=".GlobalApplicationData"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartPage"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu">
            android:label="Start Page"
            android:style= "@style/AppTheme"
        </activity>

        <activity android:name=".FileImportSelect">
            android:label="Select File Import"
            android:style="@style/AppTheme"
        </activity>

        <activity android:name=".IndividualEntry">
            android:label="Add Individual Entry"
            android:style="@style/AppTheme"
        </activity>

        <activity android:name=".EnterInterpolationPoint">
            android:label="Enter Interpolation Point"
            android:style="@style/AppTheme"
        </activity>

        <activity android:name=".DisplayInterpolatedValue">
            android:label="Display Interpolated Value"
            android:style="@style/AppTheme"
        </activity>

    </application>

</manifest>